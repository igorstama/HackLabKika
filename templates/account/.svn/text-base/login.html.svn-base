{% extends "base.html" %}

{% block title %}Log in{% endblock %}

{% block main_content %}
<div id="st">
<h2>Најави се</h2>
{% if user.is_authenticated %}
<p>Веќе си најавен(а) {{ user.username }}. Можеби сакаше да се <a href="/korisnici/odjava/">одјавиш?</a></p>
{% else %}
<form method="post" action="?next=/">
<dl>
<dt><label for="id_username">Корисник:</label>{% if form.username.errors %} <span class="error">{{ form.username.errors|join:", " }}</span>{% endif %}</dt>
<dd>{{ form.username }}</dd>
<dt><label for="id_password">Лозинка:</label>{% if form.password.errors %} <span class="error">{{ form.password.errors|join:", " }}</span>{% endif %}</dt>
<dd>{{ form.password }}</dd>
<dt><input class="button" type="submit" value="Најави се" /></dt>
</dl>
</form>

<p>Ако немате корисничко име тогаш ќе треба да се <a href="/korisnici/registracija/">регистрирате.</a></p>

{% endif %}
</div>
{% endblock %}
