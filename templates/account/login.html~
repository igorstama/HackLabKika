{% extends "base.html" %}

{% block title %}Log in{% endblock %}

{% block main_content %}
<div id="st">
<h2>Најави се</h2>
{% if user.is_authenticated %}
<p>Веќе си најавен(а) {{ user.username }}. <a href="/account/logout/">Сакате да се одјавите ?</a></p>
{% else %}
{% if message %}
	<h3>{{ message }}</h3>
{% endif %}
<form method="post" action="?next=/">
<dl>
<dt><label for="id_username">Корисник:</label>{% if form.username.errors %} <span class="error">{{ form.username.errors|join:", " }}</span>{% endif %}</dt>
<dd>{{ form.username }}</dd>
<dt><label for="id_password">Лозинка:</label>{% if form.password.errors %} <span class="error">{{ form.password.errors|join:", " }}</span>{% endif %}</dt>
<dd>{{ form.password }}</dd>
<dt><input class="button" type="submit" value="Најави се" /></dt>
</dl>
</form>

<p>Доколку немате кориснички профил, <a href="/registration/">регистрирајте се овде.</a></p>

{% endif %}
</div>
{% endblock %}
